import { utils, writeFileXLSX } from "./xlsx-export-tools.js";

/**
 * @param {string} filename
 * @returns {Promise.<void>}
 */
export async function ExportToExcel(filename) {
    // BEGIN USER CODE
    typeof utils.json_to_sheet + typeof writeFileXLSX;
    // END USER CODE
}
