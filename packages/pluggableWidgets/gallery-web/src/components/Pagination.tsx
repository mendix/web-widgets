import { observer } from "mobx-react-lite";
import { ReactElement } from "react";

export const Pagination = observer(function Pagination(): ReactElement {
    return <div className="widget-gallery-pagination">Pagination</div>;
});
