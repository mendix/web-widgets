export const ENOCONTEXT =
    "The Number filter widget must be placed inside the header of the Data grid 2.0 or Gallery widget.";

export const ECONFIG =
    "The Number filter widget can't be used with the filters options you have selected. " +
    "It requires a 'Autonumber, Decimal, Integer or Long' attribute to be selected.";

export const EATTRTYPE =
    "The attribute type being used for Number filter is not 'Autonumber, Decimal, Integer or Long'";

export const EMISSINGSTORE = "The filter store is missing. Check parent widget configuration.";

export const ESTORETYPE =
    "The type of the filter and widget is incompatible. Number filter widget must " +
    "be used with correct attribute/group type.";
