## Intent

<!-- What problem does this change solve? What user need or developer pain point drives it? -->

## Mendix XML Impact

<!-- List any XML property changes. Delete inapplicable lines.

Added properties:
- `propertyName` (type) — caption: "…", description: "…", defaultValue: …

Modified properties:
- `propertyName` — what changes and why

Removed properties:
- `propertyName` — migration path or reason for removal

None — this change has no XML property impact.
-->

## Scope

**In scope:**

- **Out of scope:**

-

## Release Impact

<!-- Check all that apply -->

- [ ] Requires `CHANGELOG.md` update
- [ ] Requires semver bump — patch / minor / major (delete inapplicable)
- [ ] Requires `editorConfig.ts` / `editorPreview.tsx` update (Studio Pro UI)
- [ ] Requires docs PR in mendix/docs (XML or behavior changes visible to Mendix users)
